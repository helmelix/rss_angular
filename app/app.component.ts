import { Component } from '@angular/core';
import { ChannelsService }     from 'app/channels/channels.service';

    @Component({
      selector: 'my-app',
      template: '<my-channels></my-channels>'
    })
    export class AppComponent {
    }
